import { View, Text, SafeAreaView } from "react-native";

const Home = () => {
	return (
		<SafeAreaView>
			<Text>This is where you see all the pokemons</Text>
		</SafeAreaView>
	);
};

export default Home;
